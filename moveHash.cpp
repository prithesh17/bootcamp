#include <iostream>
using namespace std;

string moveHash(string s) {
    int n = s.size();
    int hashCount = 0;
    
}

int main() {
    string s;
    cin >> s;
    cout << moveHash(s);
    return 0;
}
